language: c

compiler:
  - gcc

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - autoconf
    - automake
    - libsdl2-dev
    - libsdl2-image-dev
    - libsdl2-ttf-dev

script:
  - ./autogen.sh
  - ./configure
  - make
